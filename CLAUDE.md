@AGENTS.md

# Code style

- Do not write comments that just restate what the code does. Only add a comment when the WHY isn't obvious from the code itself (a non-obvious constraint, workaround, or invariant).
